name: Daily Clash Node Harvest

on:
  schedule:
    # 每天 UTC 2:00 (北京时间 10:00) 运行
    - cron: '0 2 * * *'
  # 也允许手动触发
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

env:
  PYTHONUNBUFFERED: 1
  ARCHIVE_DIR: archives
  OUTPUT_DIR: output
  MAX_ARCHIVE_FILES: 5

jobs:
  harvest:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Crawl & harvest nodes
        run: python scripts/main.py

      - name: Commit & push archives + output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add archives/ output/ -f
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🤖 Daily harvest $(date +%Y-%m-%d)"
            git push
          fi

      - name: Setup Pages
        if: success()
        uses: actions/configure-pages@v4

      - name: Deploy subscription to GitHub Pages
        if: success()
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages  # 由 upload-pages-artifact 提供
          # 实际部署 output/ 内容作为静态站点

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: output/
